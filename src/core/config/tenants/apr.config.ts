import { TenantConfig } from '../tenant.config';

const config: TenantConfig = {
  id: 'apr',
  name: 'APR Corporation',
  features: { i18n: true, ai: false }, // AI 미사용
  theme: { primaryColor: '#e11d48' }, // Rose Red
};

export default config;

